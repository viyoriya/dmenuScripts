#!/bin/bash

PROMPT="   Open bookmark "

DMENU_OPT=" -i -nb #17191C -nf #7D7F82 -sb #4C566A -sf #D8DEE9 -fn Iosevka:style=Regular:pixelsize=13"    
DMENU="dmenu -l 10 $DMENU_OPT"

file="$HOME/.config/todo/bm_url.txt"

touch "$file"
chosen=$( cat $file | $DMENU -p "$PROMPT" | sed "s/ .*//")

if [ -n "$chosen" ]; then
    xdg-open $chosen 2> /dev/null
fi

